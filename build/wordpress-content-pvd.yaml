apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wordpress-content-claim
  namespace: wordpress-omeka
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: standard
